# Exemple 3 — Rust minimal

`Cargo.toml` détecté comme langage "rust". Exécutable simulé dans
`target/release/` (place normale de `cargo build --release`).

```bash
./scripts/dev.sh check --path examples/03-rust-simple
./scripts/dev.sh build --path examples/03-rust-simple
```

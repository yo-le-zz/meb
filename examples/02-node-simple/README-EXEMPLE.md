# Exemple 2 — Node.js minimal

`package.json` détecté comme langage "node". Exécutable simulé dans
`dist/` (place normalement occupée par le binaire produit par `pkg`,
`nexe` ou similaire).

```bash
./scripts/dev.sh check --path examples/02-node-simple
./scripts/dev.sh build --path examples/02-node-simple
```
